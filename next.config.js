/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true, // This might be a good setting for development
    // webpack(config) {
    //     config.infrastructureLogging = { debug: /PackFileCache/ };
    //     return config;
    // },
};

module.exports = nextConfig;
