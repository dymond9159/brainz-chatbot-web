{
    "name": "get_explain",
    "description": "Get the explain based on the instruction with user.",
    "parameters": {
        "type": "object",
        "properties": {
            "explain": {
                "type": "string",
                "description": "Get the explain based on the instruction with user."
            }
        },
        "required": ["explain"]
    }
}